# GPT Course Template 规范文档

> 这是课程模版系统的标准规范,未来所有课程模版都应遵循此规则。

## 目录

1. [概述](#概述)
2. [JSON 结构规范](#json-结构规范)
3. [页面类型](#页面类型)
4. [内容块类型](#内容块类型)
5. [样式系统](#样式系统)
6. [最佳实践](#最佳实践)

---

## 概述

GPT Course Template 是一个杂志风格的课程模版,支持:
- 全屏翻页式导航
- 丰富的视觉效果 (玻璃态、渐变、动画)
- 15+ 种内容块类型
- 打字机效果、滚动触发动画
- 响应式设计
- 键盘/触摸/滑动操作

### 文件结构

```
templates/
├── gpt-course.template.html    # 模版文件
├── gpt-course.example.json     # JSON 示例
└── GPT-COURSE-TEMPLATE-RULES.md # 本规范文档
```

---

## JSON 结构规范

### 顶层结构

```json
{
  "templateId": "gpt-course",      // 必需: 模版ID
  "title": "课程标题",              // 必需: 页面标题
  "subtitle": "课程副标题",          // 可选: 副标题
  "topbar": {                      // 可选: 顶栏配置
    "chip": "标签文字",
    "title": "顶栏标题",
    "sub": "副标题 (如章节号)"
  },
  "pages": []                      // 必需: 页面数组
}
```

### 页面数组

每个页面是一个对象,包含以下属性:

| 属性 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `type` | string | 是 | `"hero"` 或 `"content"` |
| `accent` | string | 否 | 页面主题色 (见下方颜色系统) |
| `title` | string | 否 | 页面标题 (用于 TOC 导航) |
| `sub` | string | 否 | 页面序号 (如 "01") |
| `blocks` | array | 是* | 内容块数组 (*content 类型必需) |

---

## 页面类型

### 1. Hero 页面 (首页)

完整的 Hero 页面配置:

```json
{
  "type": "hero",
  "accent": "purple",
  "badge": {
    "icon": "star",              // 图标名称
    "text": "徽章文字"
  },
  "title": "大标题",
  "subtitle": "副标题 (支持打字机效果)",
  "subtitleTyping": true,        // 是否启用打字机效果
  "features": [
    { "icon": "layers", "text": "特性1" },
    { "icon": "database", "text": "特性2" }
  ],
  "cta": {
    "primary": { "text": "主按钮", "icon": "play" },
    "secondary": { "text": "次按钮", "icon": "list" }
  },
  "stats": [
    { "value": "2018", "label": "统计标签" },
    { "value": "117M", "label": "参数" }
  ]
}
```

### 2. Content 页面 (内容页)

```json
{
  "type": "content",
  "title": "页面标题",
  "sub": "01",
  "accent": "cyan",
  "blocks": [
    // 内容块数组
  ]
}
```

---

## 内容块类型

### 1. section-label (章节标签)

```json
{
  "type": "section-label",
  "text": "CHAPTER 01"
}
```

输出: 带有前置装饰线的大写标签文字

---

### 2. section-title (章节标题)

```json
{
  "type": "section-title",
  "text": "大标题文字"
}
```

输出: 衬线字体的大号标题

---

### 3. section-desc (章节描述)

```json
{
  "type": "section-desc",
  "text": "描述性段落文字"
}
```

输出: 灰色的描述文字

---

### 4. prose (段落文本)

```json
{
  "type": "prose",
  "paragraphs": [
    "第一段文字...",
    "第二段文字,支持 <hl class=\"hl-cyan\">高亮</hl> 标记",
    "使用 <em class=\"em-pink\">强调</em> 文字"
  ]
}
```

**高亮样式:**
- `hl-cyan` - 青色高亮
- `hl-pink` - 粉色高亮
- `hl-green` - 绿色高亮
- `hl-amber` - 琥珀色高亮
- `hl-red` - 红色高亮
- `hl-purple` - 紫色高亮

**强调样式:**
- `em-cyan`, `em-pink`, `em-green`, `em-amber`, `em-purple`

---

### 5. cards-grid (卡片网格)

```json
{
  "type": "cards-grid",
  "columns": 3,                   // 2 或 3 列
  "cards": [
    {
      "icon": "🔄",               // 表情或文字
      "iconColor": "cyan",        // cyan|pink|green|amber|purple
      "title": "卡片标题",
      "subtitle": "副标题",
      "text": "描述文字"
    }
  ]
}
```

---

### 6. keyline (关键要点)

```json
{
  "type": "keyline",
  "dot": "K",                     // 标记文字 (1-2字符)
  "text": "重要内容,支持 <em class=\"em-cyan\">内联样式</em>"
}
```

输出: 带有彩色标记的重点内容框

---

### 7. think-box (思考盒)

```json
{
  "type": "think-box",
  "icon": "💡",
  "title": "思考题",
  "text": "问题内容..."
}
```

输出: 琥珀色边框的思考/问题框

---

### 8. example-box (示例代码框)

```json
{
  "type": "example-box",
  "color": "cyan",                // cyan|pink|green|amber
  "icon": "💻",
  "title": "代码示例",
  "code": "<span class=\"keyword\">def</span> <span class=\"function\">hello</span>():\n    <span class=\"keyword\">print</span>(<span class=\"string\">\"Hello\"</span>)",
  "note": "可选的注释说明"
}
```

**代码高亮类:**
- `.comment` - 注释 (灰色)
- `.keyword` - 关键字 (紫色)
- `.string` - 字符串 (绿色)
- `.number` - 数字 (橙色)
- `.function` - 函数名 (蓝色)
- `.variable` - 变量 (红色)

---

### 9. math-block (数学公式块)

```json
{
  "type": "math-block",
  "title": "公式推导",
  "steps": [
    { "step": "步骤1:", "eq": "x = 1" },
    { "step": "步骤2:", "eq": "y = x + 1", "comment": "可选注释" }
  ],
  "result": "最终结果: y = 2"
}
```

---

### 10. callout (信息框)

```json
{
  "type": "callout",
  "title": "标题",
  "tag": "TIPS",                  // 可选标签
  "items": [
    "列表项 1",
    "列表项 2"
  ]
}
```

---

### 11. accordion (折叠面板)

```json
{
  "type": "accordion",
  "style": "default",             // default 或 answer
  "badge": "详解",
  "title": "点击展开",
  "content": [
    "段落1",
    "段落2"
  ]
}
```

---

### 12. formula-box (公式展示框)

```json
{
  "type": "formula-box",
  "label": "核心公式",
  "formula": "<span class=\"formula-func\">f</span>(<span class=\"formula-var\">x</span>) <span class=\"formula-op\">=</span> ...",
  "desc": "公式说明"
}
```

**公式高亮类:**
- `.formula-var` - 变量 (蓝色)
- `.formula-func` - 函数 (紫色)
- `.formula-op` - 运算符 (青色)

---

### 13. diagram-container (图表容器)

```json
{
  "type": "diagram-container",
  "svg": "<svg>...</svg>"
}
```

支持 SVG 动画类:
- `.svg-fade.d1` ~ `.svg-fade.d5` - 延迟淡入
- `.svg-path` - 路径描边动画

---

### 14. quote-box (引用框)

```json
{
  "type": "quote-box",
  "text": "引用内容...",
  "author": "— 作者名"
}
```

---

### 15. sticky-note (便签)

```json
{
  "type": "sticky-note",
  "color": "yellow",              // yellow|pink|blue|green|purple|orange
  "title": "便签标题",
  "text": "便签内容..."
}
```

---

### 16. page-transition (页面过渡提示)

```json
{
  "type": "page-transition",
  "icon": "→",
  "text": "下一页将介绍..."
}
```

---

## 样式系统

### 颜色主题 (accent)

每个页面可以设置不同的主题色:

| 值 | 主色调 | 使用场景 |
|-----|--------|---------|
| `purple` | 紫色渐变 | 开篇、重要章节 |
| `cyan` | 青色 | 技术内容、概念解释 |
| `pink` | 粉色 | 创意、灵感 |
| `green` | 绿色 | 成果、成功 |
| `amber` | 琥珀色 | 警告、注意事项 |

### 动画效果

所有内容块支持滚动触发动画:
- 进入视口时自动添加 `.visible` 类
- 动画由 CSS `transition` 控制
- 建议动画时长: 0.5s - 0.8s

---

## 最佳实践

### 1. 页面结构

```
Hero 页 (1页)
├── 标题 + 副标题
├── 特性列表 (3-5项)
└── 统计数据 (3-4项)

内容页 (5-10页/章节)
├── section-label + section-title
├── 核心内容块 (prose/cards/keyline)
├── 详解块 (example/math/accordion)
└── page-transition (可选)
```

### 2. 内容密度

- 每页 3-5 个内容块
- 避免长段落 (超过4行考虑拆分)
- 重要内容使用 keyline 或 callout 强调

### 3. 视觉层次

```
section-label  ← 最淡 (小写)
section-title  ← 最醒目 (大号衬线)
section-desc   ← 次要 (灰色)
prose          ← 正文
keyline        ← 重点强调
```

### 4. 颜色使用

- 每页只用一个 accent 颜色
- 高亮标记 (hl-*) 不要过度使用
- 相邻页面使用不同 accent 增加视觉变化

### 5. 示例代码

- 保持简短 (5-15行)
- 使用语法高亮类
- 添加注释说明关键点

---

## 渲染器要求

模版渲染器 (`unified-course-renderer.js`) 需要支持:

1. 解析 JSON 配置
2. 根据 block type 生成对应 HTML
3. 初始化 Intersection Observer 实现滚动动画
4. 初始化键盘/触摸导航
5. 渲染 KaTeX 数学公式 (如使用)

---

## 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| 1.0 | 2025-01 | 初始版本,基于 GPT-1 课程抽象 |

---

*本文档为课程模版系统的标准规范,所有新模版应参考此规范进行设计和实现。*
